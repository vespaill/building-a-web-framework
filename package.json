{
    "scripts": {
        "start:db": "json-server -w db.json",
        "start:parcel": "parcel index.html"
    },
    "dependencies": {
        "axios": "^0.19.2"
    },
    "devDependencies": {
        "typescript": "^3.9.2"
    }
}
